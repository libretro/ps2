Lower-table census, battery 1: integer/move exact; Q-pipeline anomaly open
==========================================================================
14 lower ops x 400 cases x {default, exactdiv}, full-state diff.

Verified exact (all state, both configs):
  IADD ISUB IADDI IAND IOR  -- the integer ALU is bit-exact between
    engines (executed-effect confirmed: results land in VI)
  MOVE MR32 MTIR MFIR       -- register moves exact (MTIR effect
    confirmed, 371/400 nonzero results)

Memory family -- VERIFIED EXACT (memory-seeded battery):
  LQ SQ ILW ISW LQI SQI LQD SQD ILWR ISWR, 400 cases each against
  per-case deterministic pseudorandom VU data memory
  (ps2_audit_vu_memfill), with full-state register comparison AND a
  post-run memory hash (ps2_audit_vu_memhash) per engine. All rows
  bit-identical on both. Executed-effect held throughout: loads
  carried nonzero varied data in 400/400, stores changed the memory
  hash away from the pure-fill baseline in 400/400 (the check that
  distinguishes agreeing stores from agreeing inaction), ILW landed
  nonzero VI results in 400/400. The battery-1 LQI/SQI weak rows are
  superseded at full strength.

Battery 2 -- flag ops, R ops, branches: ARCHITECTURALLY EXACT
  24 ops x 400 cases: IADDIU ISUBIU, RINIT RGET RNEXT RXOR, FSEQ
  FSSET FSOR FMEQ FMOR FCEQ FCAND FCOR FCSET FCGET, IBEQ IBNE IBLTZ
  IBGTZ IBLEZ IBGEZ B BAL. Branch takenness exercised both paths
  (146-275 taken per row, marker-instruction discrimination) and
  every observable result -- VF, VI results, branch markers, flag-op
  outputs -- is bit-identical between engines.

  Rig fix banked: the recompiler's status-flag instances hold an
  internal layout, and seeding them with the raw architectural
  value mangled the block-end normalize; the exec export now seeds
  through the same denormalization the core applies to CFC2-written
  status (mVUallocSFLAGd's mapping). Before the fix this manufactured
  phantom divergence in every op; after it, flag-reader RESULTS are
  exact.

  Representation notes (architecture-equal, recorded for savestate
  fidelity review, not correctness findings):
  - R register: interpreter stores hardware format
    (0x3F800000|mantissa), recompiler stores the bare 23-bit
    mantissa and re-ors on RGET; RGET results are identical.
  - Raw VI[REG_STATUS] after a recompiled block that never computed
    flags differs from the seed in single sticky bits (the rec exit
    path's sticky handling); every flag READ (FSAND FSEQ FSOR FCGET
    results) agrees, so the difference is invisible to programs.

Battery 3 -- register jumps and VIF reads:
  JR and JALR exact (0/400 each) with landing-pattern verification
  across four target pairs: every pattern shows the branch delay
  slot executing before the landing point, identically in both
  engines (e.g. target pair 4 yields markers 1,0,0,1 -- the
  delay-slot marker plus the landing tail), and JALR's link write
  agrees. XTOP/XITOP agree structurally (both engines read the same
  vif top/itop globals) but coverage is thin: the values were zero
  at both the boot and in-game sample points, so the address/masking
  of the read is unexercised; recorded as weak rows, semantic risk
  minimal. XGKICK is out of rig scope -- its effect is a GIF-path
  side transfer, which needs GIF capture comparison, not register
  diffing.

LOWER TABLE STATUS: complete. Integer ALU, immediate ALU, moves,
memory family (seeded), Q pipeline (exact under exactdiv, oracle
fixed), R ops, flag ops, branches with delay-slot verification, and
register jumps -- all architecturally exact. Remaining VU items are
the weak-coverage notes above and XGKICK's out-of-scope transfer.

Q pipeline -- RESOLVED (this commit):
  The "lane-selection anomaly" was a rig readback artifact: the
  recompiler retires committed Q into VI[REG_Q] (microVU_Branch
  block-end flush), not the q field the readback compared, so the
  Q column was seed-vs-computed. The lane-identified probe (distinct
  exponent per lane, 16 fsf/ftf combos) then showed 16/16 bit-exact
  agreement -- no decode disagreement exists. The exec export now
  mirrors the recompiler's committed VI[REG_Q]/VI[REG_P] into the
  q/p fields so the readback slots are engine-agnostic.

  Real Q census (400 cases): default DIV 183, SQRT 106, RSQRT 184
  divergent -- one-ulp chop-vs-model plus overflow clamp-vs-saturate,
  the classes exact divide exists for; wire proven live (183/400
  def-vs-exactdiv rec deltas). Under exactdiv: DIV 0, SQRT 0,
  RSQRT 7 -- and the RSQRT residual was an ORACLE bug: every case
  was ft = -0, where the interpreter's hand-coded zero-denominator
  path XORed both signs while the hardware-verified model, the
  exact stub, and the recompiler's default emission all agree the
  sqrt stage strips the denominator sign (saturation by numerator
  sign alone). With the interpreter corrected: DIV 0, SQRT 0,
  RSQRT 0 -- the exact-divide option is bit-exact for the entire
  Q pipeline, its first accuracy verification.

Rig lessons banked this battery:
  - The q field sits at regs_io offset 656 (after the 16-byte ACC at
    640); the first analysis read 644 -- inside ACC -- and produced
    an all-zero Q column that the executed-effect check (q_out must
    differ from q_in) exposed. That check is now part of the
    standard battery integrity set alongside files-must-differ and
    known-answer splits.

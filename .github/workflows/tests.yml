name: Tests

# The suites under tests/ had no CI at all: sixteen of them, each with a
# build.sh, and nothing ran any of them. They cover the interpreter and
# recompiler against the PS2 hardware tests, the emitter, the settings
# path, the CDVD readers, the MTVU ring and the fault handler -- which is
# to say most of the places a change has broken this core before.

on:
  push:
    branches: [master, libretroization]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

jobs:
  suites:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: plain
            sanflags: ''
          # Twelve of the sixteen build from SANFLAGS, so the sanitizers
          # cost a rebuild rather than a separate harness. The other four
          # -- faultstress, spsc, mtvuring, gs -- are the concurrency ones
          # and run TSan themselves every time, which is the sanitizer
          # that finds anything there; they simply repeat in these two
          # rows.
          - name: asan
            sanflags: '-fsanitize=address -fno-omit-frame-pointer'
          - name: ubsan
            sanflags: '-fsanitize=undefined -fno-omit-frame-pointer'
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install what the suites build against
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3 liblzma-dev zlib1g-dev libgl-dev

      # run-all.sh finds every directory with a build.sh, so a suite added
      # later is covered here without touching this file.
      - name: Run the suites
        env:
          SANFLAGS: ${{ matrix.sanflags }}
        run: sh tests/run-all.sh

  # The hardware-test suites compare against the PS2 test images and check
  # far less without them. A green tick above should not be read as those
  # having run, so this says which did what.
  report:
    name: what ran
    needs: suites
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: List the suites and what each needs
        run: |
          for d in tests/*/build.sh; do
            s=$(dirname "$d"); s=${s##*/}
            if grep -q 'PS2AUTOTESTS\|PS1TESTS' "$d"; then
              echo "$s: compares against the PS2/PS1 hardware test images; without"
              echo "    PS2AUTOTESTS or PS1TESTS set it checks only that it builds"
            else
              echo "$s: self-contained"
            fi
          done
